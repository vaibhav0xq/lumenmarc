{
  "name": "lumenmarc",
  "version": "1.0.0",
  "private": true,
  "description": "LumenMarc — the fair-value and integrity layer for Coinbase Tokenized Stocks on Base.",
  "author": "Vaibhav (https://github.com/vaibhav0xq)",
  "license": "MIT",
  "homepage": "https://github.com/vaibhav0xq/lumenmarc#readme",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/vaibhav0xq/lumenmarc.git"
  },
  "bugs": {
    "url": "https://github.com/vaibhav0xq/lumenmarc/issues"
  },
  "packageManager": "pnpm@10.26.1",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "preinstall": "sh -c 'rm -f package-lock.json yarn.lock; case \"$npm_config_user_agent\" in pnpm/*) ;; *) echo \"Use pnpm instead\" >&2; exit 1 ;; esac'",
    "build": "pnpm run typecheck && pnpm -r --if-present run build",
    "typecheck:libs": "tsc --build",
    "typecheck": "pnpm run typecheck:libs && pnpm -r --filter \"./artifacts/**\" --filter \"./scripts\" --if-present run typecheck",
    "build:vercel": "node scripts/vercel-build.mjs"
  },
  "dependencies": {
    "@replit/connectors-sdk": "^0.4.1"
  },
  "devDependencies": {
    "prettier": "^3.9.6",
    "typescript": "~5.9.3"
  }
}
